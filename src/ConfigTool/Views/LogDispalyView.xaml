<UserControl x:Class="ConfigTool.Views.LogDispalyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ConfigTool.Views"
             mc:Ignorable="d" 
             d:DesignHeight="175" d:DesignWidth="1500">
    <Grid>
        <ListView x:Name="LogListView" ItemsSource="{Binding LogList}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Data" Width="150"  DisplayMemberBinding="{Binding Date}"/>
                    <GridViewColumn Header="Time" Width="150" DisplayMemberBinding="{Binding Time}"/>
                    <GridViewColumn Header="Source" Width="150" DisplayMemberBinding="{Binding TaskName}"/>
                    <GridViewColumn Header="Event" Width="1000" DisplayMemberBinding="{Binding Event}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
